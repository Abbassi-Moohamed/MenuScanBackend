# Ensure devDependencies (TypeScript, @types, vitest, …) are always installed.
# Render and other hosts run `npm install` with NODE_ENV=production, which would
# otherwise omit devDependencies and break `npm run build` (missing @types/node).
include=dev